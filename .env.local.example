# Local development environment variables
# Copy this to .env.local and adjust for your setup

export SSH_HOST=ssh_host
export SSH_USER=root
export DOCKER_USERNAME=root
export DOCKER_TOKEN=dkr_token

# Local development URLs
export VITE_API_URL=http://localhost:4000
export NODE_ENV=development
export BACKEND_PORT=4000

export REDIS_PASSWORD=password1234
export REDIS_READONLY_USER=readonly
export REDIS_READONLY_PASSWORD=pwd134

# Redis connection for local backend (connecting to localhost:6379)
export REDIS_URL=redis://admin:${REDIS_PASSWORD}@localhost:6379
export REDIS_HOSTS=local:${REDIS_READONLY_USER}:${REDIS_READONLY_PASSWORD}@redis:6379

VITE_API_URL=http://localhost:4000
VITE_DEBUG=true
VITE_SCAN_PERF=false

